declare function detectEnvironment(): "tauri" | "wxp" | "unknown";
declare function invoke<T = unknown>(cmd: string, args?: Record<string, unknown>): Promise<T>;
declare class Channel<T = unknown> {
    private channel;
    private _onmessage?;
    private pendingId;
    constructor(onmessage?: (message: T) => void);
    private initializeTauriChannel;
    private initializeWxpChannel;
    private initializeWithRetry;
    get onmessage(): ((message: T) => void) | undefined;
    set onmessage(handler: ((message: T) => void) | undefined);
    toJSON(): string;
}
declare const webviewBridge: {
    detectEnvironment: typeof detectEnvironment;
    invoke: typeof invoke;
    Channel: typeof Channel;
};

export { Channel, webviewBridge as default, detectEnvironment, invoke, webviewBridge };
